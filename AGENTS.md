# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Desktop prototype testing

- Keep `Bean.command` as the directory-local, double-clickable macOS test launcher.
- Keep `Brizo.command` as the current branded, double-clickable macOS launcher; retain `Bean.command` only as a backwards-compatible launcher so existing user shortcuts do not break.
- The desktop test window must load `dist/client/index.html` directly through Electron and must not depend on Vite or another local web server.
- Preserve `npm run desktop:smoke` as the automated check for the local-file desktop runtime.
- External web pages in the desktop prototype must use an isolated Electron `WebContentsView`; keep Node integration disabled, context isolation and sandboxing enabled, and remote permission requests denied by default.
- Preserve `npm run desktop:browser-smoke` as the automated external-page runtime check.
- Use `npm run desktop:run` for normal local launches against an existing `dist`; `Brizo.command` and legacy `Bean.command` build only when `dist/client/index.html` is missing. Keep build-before-run behavior in smoke, distribution, and explicit `npm run desktop` workflows.
- Preserve `npm run perf:startup` as the repeatable local cold-process shell benchmark; treat its Chrome headless timing as directional rather than a like-for-like page-load score.

## Durable interaction decisions

- Use the project-root `background.png` as the new-tab page background, preserving its aspect ratio while filling and cropping to the canvas (`cover`), with the image layer at 10% opacity (90% transparent).

- Do not show algorithm, provider, or model labels above generated search answers. Keep the answer surface focused on the response, sources, and follow-up topics.
- In the Scout AI result state, shrink the bottom composer to approximately two-thirds of its idle width. Place sources above the generated answer, show only the three highest-ranked authoritative/relevant sources as horizontal cards by default, and use stacked circular site icons plus an explicit expand/collapse control to reveal the remaining sources downward.
- Place a compact icon-only action row below each completed Scout AI answer for copying the full result, downloading a clean PDF, and sharing by copying a recoverable Brizo search address. Generate five follow-up questions per completed search and preserve all five in local result snapshots.
- Make the complete Scout result canvas the vertical scroll container rather than only the centered reading column. Scrolling must work over both side gutters as well as the answer body, with the scrollbar at the far-right edge of the page; keep the centered result width and bottom composer visually unchanged and reserve enough trailing space so the composer never covers the final follow-up.
- Never render a repeated level-one Markdown question heading (`# question`) inside a Scout AI answer because the result header already owns the query title. Keep Zhihu eligible as internal grounding evidence but omit Zhihu from visible source cards, copied result source lists, and exported PDF source lists. Increase Scout result-reading typography by 2 px while keeping the compact source presentation at its existing size.
- Determine search-result and autocomplete language from the language the user actually types, never from IP country or geographic location. Chinese input must keep query planning, search-engine queries, result titles and snippets, vertical cards, suggestions, generated answers, and follow-up questions in Chinese; filter Japanese kana, Korean Hangul, and pure non-Chinese results. Geographic signals may narrow local intent but must never select another display language.
- Order Scout sources in three strict authority tiers before presenting or assigning citation numbers: first-party official websites and government/institutional primary sources first, broadly recognized authoritative media second, and all other sources third. Within each tier, preserve relevance, cross-provider agreement, and freshness ranking.
- Use “Brizo Scout AI” as the product-facing name for Brizo search results; do not display the former “Brizo Explore” or “Brizo 搜索” labels.
- Insert every newly created blank tab at the far left of the tab list. Each blank tab owns an independent in-memory search session, including its prompt, selected model, attachments, loading/result state, sources, and follow-up topics; preserve it while switching tabs until that tab is manually closed. After a search starts, label that tab `搜索: <query>`.
- Style the bookmark library as a compact GitHub file explorer with no view selector and no bookmark search field. Show the real bookmark tree directly, use the gold sampled from the Brizo logo for folder icons, and preserve real rounded-rectangle favicons for webpage rows.
- Keep the new-tab plus button at the far left of the horizontal tab strip. All page tabs begin to its right with a small visual gap.
- Start each Brizo session with exactly one blank new tab. Never pre-populate the horizontal tab strip with demo, research, or sample webpage tabs.
- Replace the bottom-left local account identity row with a bare “模型护航” icon placed directly in the bookmarks rail. Do not render an outer dock/card, divider, background, border, label, status copy, or caret around the trigger. Its menu still expands upward to choose among bound model/API providers, marks the default provider, and always offers “添加模型”; do not show the legacy A/Alex identity or four-dot account action there.
- Use project-root `hermes logo.svg` for the bare 模型护航 trigger at 21 px, centered inside a 30 px click target in the bookmarks rail. Give every bound model/API row in its upward menu a separate edit action that opens 大模型护航 directly in that provider's edit state, without exposing its saved key. Close the menu on any pointer click outside it or on Escape.

- On the Brizo start/new-tab page, submitting a natural-language query transitions into an answer view: the Brizo logo and greeting disappear, the existing Border Beam composer animates to a fixed bottom position, and a scrollable result area appears above it immediately with a loading state, synthesized answer, and clickable sources. URL-like input continues to navigate directly. Real desktop searches use Brizo's built-in Scout pipeline with Serper.dev and 博查 as complementary professional retrieval sources, and a bound fast OpenAI-compatible model for planning and synthesis; unavailable or unconfigured services must be shown honestly and must never be replaced with fabricated live results.
- Keep “大模型护航” in the rightmost settings menu as the local API credential manager. Support multiple OpenAI-compatible API entries, a leading single-choice default control, encrypted desktop-only key storage, masked key display, model discovery from the configured `/models` endpoint, and deletion. Do not show a model selector inside the new-tab or result composer; use the default provider's highest-ranked compatible model automatically. Never embed API keys in renderer source, build artifacts, preview fixtures, or browser storage.
- Let users edit every saved model/API provider from “大模型护航”. Editing may change its name, Base URL, default status, or replace the API Key. Never return an existing plaintext key to the renderer: display a fixed `********` mask, treat an empty key field as “keep the encrypted key,” and replace it only when the user enters a new value.
- Rank each provider's discovered models so fast variants are the default—prefer names containing `flash`, then `instant`, `turbo`, `mini`, `fast`, `chat`, `lite`, or `haiku`, while deprioritizing reasoning/thinking/pro/max variants. Brizo Scout AI uses the bound fast model to plan up to three search-engine queries, retrieves Serper.dev and 博查 concurrently, fuses and deduplicates their real results, streams an answer constrained to numbered citations, and generates five follow-up questions. For now, every new Scout search uses fast depth and the composer shows no automatic/fast/balanced/deep selector; slower modes remain unavailable even to stale renderer payloads. Never silently replace failed professional retrieval with a direct-model answer or fabricate sources; use keyless public retrieval only when no professional search service is configured and disclose that degraded mode in the UI.
- For entity, product, organization, community, documentation, download, login, or official-site intent, reserve a deterministic official-site query in the retrieval plan. Treat an exact distinctive brand token matching the registrable domain (including short alphanumeric brands such as `v0` → `v0.app`) as first-party, retain that official result even when its page language differs from the user's answer language, and always place it before media, reviews, community mirrors, and other third-party commentary. Keep the generated answer and all UI copy in the user's input language.
- Automatically route short, unambiguous existence and yes/no factual questions through Scout's fast path: skip the model planning round-trip, search only the user's exact query, do not scrape full webpages, cap synthesis for a concise answer, and overlap generation of the five follow-up questions with the visible answer stream. Complex, current, comparative, medical, legal, and investment questions may still receive model query planning, but remain on fast retrieval depth without full-page scraping.
- Seed Serper.dev and 博查 credentials only from the gitignored project-root `search-keys.local.json` or process environment into Electron `safeStorage`. Never return plaintext or encrypted service keys to the renderer, and never include them in source, build output, fixtures, browser storage, or search snapshots.
- Make default-model ranking provider-aware and text-chat-safe. Prefer models matching the bound provider family (for example Qwen for a Qwen provider), never treat the `MiniMax` brand substring as the `mini` speed token, strongly demote speech/audio/ASR/TTS/realtime/embedding/image/OCR/translation-only models, and prefer stable aliases over date-suffixed snapshots when otherwise tied. Keep non-default discovered models available for explicit user selection.

- Hide the native Brizo title bar in the main macOS window. Place the real macOS close, minimize, and fullscreen traffic-light controls at the upper-right end of the horizontal page-tab strip, where they replace the former search and open-tab-count controls; keep them right-aligned as the window resizes and preserve draggable blank toolbar regions.
- Start every Brizo browser session with the Brizo default new-tab/start page selected: the centered logo, “你想去往哪里？” heading, and Border Beam command surface must be the first visible page, with an empty address field and the external browser view hidden until the user opens or selects a webpage.
- New page tabs open Brizo's own default new-tab surface instead of loading an external placeholder. Center the supplied graphic logo above “你想去往哪里？” and a vertically tight Perplexity-CLI-like command surface. Keep file/image insertion and existing-tab context selection at bottom-left, with only the submit button at bottom-right; use the same horizontal edge inset and bottom inset for the far-left insertion button and far-right submit button. Render the input's animated edge with the installed `border-beam` package using its full-size colorful rotating beam, light theme, and 70% strength; keep the surface's neutral-gray 1 px border underneath and do not substitute a hand-built pseudo-element glow. Preserve the package's reduced-motion behavior.
- Size the two lower-left new-tab composer tool buttons and the lower-right submit/forward button at 30 px, approximately 30% smaller than their former 42 px size. Scale their primary icons to 14–15 px while preserving the established outer edge and bottom alignment.
- Keep the new-tab text input clean: do not show a terminal-style `›_` prompt marker before its content.
- When the pointer hovers over the new-tab/result composer submit button, or the button receives keyboard focus, reveal the exact label “Ask Brizo” with a short upward-floating motion above the button; hide it at rest and respect reduced-motion preferences.
- On an idle new-tab page, show the exact italic English tagline “Brizo, navigate beyond the known.” in small type, centered 22 px above the bottom edge. Hide it once the tab enters a loading or result state.
- Place a circular local search-history button in the lower-left corner of every Brizo new-tab surface. Clicking it opens the locally stored search history upward; constrain long history to an internally scrollable panel, close it on outside click or Escape, and fill/focus the composer without auto-submitting when a history item is selected.
- Persist a local result snapshot after every completed Brizo search, including the rendered answer body, sources, follow-up questions, result status, and necessary display metadata, but never credentials, attachment contents, or runtime objects. Selecting a history entry with a snapshot must restore that saved result and tab title directly without calling AI or performing web retrieval again; legacy entries without snapshots may only refill the query and must not auto-search.
- While typing in the new-tab command field, show exactly three ranked autocomplete suggestions directly below it when enough input is available. Suggestions may be phrases, complete questions, or completed website URLs; place the most likely suggestion first and allow a click to submit it immediately.
- Rank new-tab autocomplete using locally stored question history (prefix match, frequency, and recency), then live online search suggestions. Do not fabricate mechanical sentence-template fallbacks such as appending “是什么” to an already complete question. Debounce online requests, support both Electron and the local browser preview, and never use or expose a bound AI API key for autocomplete.
- At Brizo startup, perform one IP-country lookup and cache the result for the session. Use Baidu Suggest when the detected country is mainland China (`CN`); use Bing Suggest elsewhere. Do not repeat the IP lookup for each keystroke. Cache suggestion responses for five minutes and back off after rate-limit responses.
- Randomize the new-tab heading from 30 complete Chinese sentences. On each Brizo launch, the initial tab uses the corresponding sentence beginning with “今天”; additional new tabs opened in that same session use the paired complete sentence without “今天”. Keep each tab's selected sentence stable for its lifetime.
- Style the new-tab submit/confirm button in pale gold at rest; on hover or keyboard focus, change it to the Brizo logo gold (`#a58c5e`).
- Keep all open page tabs in one horizontal tab strip directly above the address toolbar. The bookmarks library remains in the left rail; do not reintroduce a separate middle tab rail. Use restrained 8 px top corners and compact 11 px outward-curving lower-left and lower-right shoulders so the active tab flows naturally into the webpage surface without looking overly round. Match the active tab background to the active webpage's computed background color; keep active tabs at 37.4 px, reduce inactive tabs to 30.6 px, and use the slightly deeper pale gray `#e9ebe7` for inactive tabs. Do not draw a hard outline around the active tab or unified webpage frame; retain the separate page-color fill path and use only a subtle, soft, outward-spreading pale-gray shadow to distinguish these opaque surfaces from the shell.
- The leftmost bookmarks/spaces sidebar rests as an icon-only rail. Expand it immediately when the pointer enters any part of the rail, and collapse it immediately when the pointer leaves the sidebar. Do not use a click toggle, header control, boundary target, or external-page interaction to control this state. Use a fast, smooth transition for both states.
- Keep folder icons, webpage favicons, row heights, and vertical spacing identical between the collapsed and expanded sidebar states. Except for the top Brizo logo, use a 20 px scale for bookmark icons with a subtle 1.5 px down-right hard shadow (zero blur), preserving the same icon-column alignment in both states.
- When hover expands the bookmark sidebar, keep every bookmark/folder icon and the bare model-guard icon fixed in place. Expansion may only reveal the text/content area to their right; do not swap row padding, grid columns, centering, or vertical spacing between collapsed and expanded states.
- Pin the top Brizo logo mark to the bookmark rail's upper-left corner with equal top and left inset. Sidebar expansion must never move the mark; only reveal the wordmark to its right.
- Do not show disclosure chevrons before bookmark folders. Align every folder icon to the same left edge and 22 px icon column as webpage favicons at the corresponding hierarchy depth.
- Hovering a bookmark folder opens its contents in a flyout immediately to the right. Nested folders continue as rightward cascading flyouts; leaving the complete sidebar/flyout tree closes the cascade. Every flyout level uses the same fixed 5% transparency (95% opacity), without increasing transparency as nesting deepens, and floats over an unchanged visual snapshot of the current webpage; do not resize, shift, crop, or replace the page with the loading placeholder while the cascade is active.
- Treat sidebar expansion and folder activation as separate phases: the collapsed 60 px rail only initiates the 180 ms sidebar expansion, and folder hover/click activation begins only after that expansion completes. Then apply a very short 90 ms hover-intent delay before opening bookmark-folder flyouts, cancelling the pending open if the pointer leaves first. This prevents collapsed-rail and expanded-sidebar flyout geometry from overlapping.
- Whenever the pointer leaves the complete bookmark sidebar/flyout tree, wait 500 ms before collapsing the sidebar, whether or not a folder flyout is open. Keep the sidebar and any flyouts visible during that grace period, and cancel the pending collapse if the pointer re-enters anywhere in the tree.
- When a webpage bookmark is selected from any flyout level, synchronously close the entire folder cascade, discard its webpage snapshot, restore the live external `WebContentsView`, and only then select or navigate the destination tab; never leave the Logo loading placeholder in place of the destination page.
- Opening any Brizo-owned shell overlay above an external webpage—including address suggestions, the settings menu or settings panels, page summary, AI panels, and bookmark cascades—must never expose the white renderer host. Keep the live webpage visible until a current full-viewport snapshot is painted, place the overlay above that snapshot, and on close restore the retained live `WebContentsView` before removing the snapshot. The webpage must not resize, navigate, reload, or lose DOM and scroll state during this handoff.
- Limit every bookmark-folder flyout to 50% of the viewport height. When its contents exceed that limit, anchor the flyout 8 px above the window bottom so it grows upward, and scroll excess items inside the flyout; shorter flyouts remain aligned with their triggering folder when space permits.
- Keep every bookmark-folder flyout exactly 150 px wide, matching the fully expanded bookmark sidebar at every cascade level.
- Keep the expanded leftmost bookmark sidebar at 150 px and the collapsed icon rail at 60 px. Vertically center every webpage bookmark title against its rounded-rectangle favicon.
- The leftmost rail is the complete bookmarks library, never a page-tab list. Do not show a Folders/Smart or 收藏夹/智能收藏夹 switch; preserve and display every imported level directly (folder → subfolder → … → webpage title). Vertically center the complete root bookmark list in the available sidebar height when it fits, while overflowing trees start safely at the top and scroll normally. Imported bookmarks stay in this rail and only create a page tab after the user opens one.
- Treat browser bookmark-bar aliases—including `书签栏`, `收藏夹栏`, `Bookmarks bar`, `Favorites bar`, and `Favourites bar`—as one structural root and never show them as visible folders. Lift their direct webpages and child folders into the bookmark sidebar root, render those direct webpages first at the same level as the folders, and keep `其他书签` and `移动设备书签` as visible first-level folders. Migrate already-saved paths and manual folder orders automatically.
- Within each imported folder, order webpage bookmarks by the most recent available creation/modification timestamp first; preserve the source browser's original folder order. Let users drag webpage bookmarks to reorder them or move them into another folder, and drag folders to reorder or nest them. Persist manual organization locally and treat it as authoritative until changed again.
- Give webpage bookmark rows a subtle translucent-white hover surface with vertically inset top and bottom edges, a flat left edge, and a fully rounded semicircular right end. Root-level webpage hover surfaces must extend fully to the browser's left frame line. Use a subtle left-origin reveal, keep the normal arrow cursor, and never show a six-dot drag handle; drag-and-drop organization remains available directly from the row.
- Give bookmark folders the same translucent-white hover surface and left-edge behavior as webpage rows. Folder flyouts use the same warm off-white as the bookmark rail at 95% opacity, while webpage and subfolder rows inside flyouts use compact 36 px vertical rhythm.
- Start every new browser session with the complete bookmark folder tree collapsed, regardless of the previous session's expanded folders. Every imported webpage bookmark should carry and display a 5 px rounded-rectangle site favicon. Preserve and show an imported favicon first; only replace it after it fails to load or its intrinsic resolution is below the 20 px display size and a higher-quality site-declared favicon has been validated. On first entry into the bookmark library, resolve missing or low-resolution favicons strictly by folder depth—complete the root level before the second level, then the third, and so on. Do not use Bing Favicon or Google S2 fallbacks; retain the existing icon or show the transparent fallback when no verified site icon is available.
- Preserve the original semantic bookmark icon language as a reusable library: thin SVG outlines at rest, switching to the matching filled glyph on hover or selection. Every icon needs a stable semantic ID, Chinese and English names, category, and search keywords so Smart organization and future features can select icons without coupling to a screen.
- Keep the horizontal top tab strip compact and divider-free. Tabs remain bottom-aligned above the address toolbar so the 10%-taller active tab rises upward without shifting the toolbar or webpage.
- Keep the address toolbar and complete active-page vector contour one layer above every inactive tab, including the fixed Brief tab, with the active tab's interactive content one further layer above that shared contour. Inactive tab fills must never cover either shoulder of the active contour.
- When the user clicks a different existing page tab or Brief, animate the complete active vector contour between the old and new tab over 220 ms with an ease-out slide and width interpolation. When tabs are added, closed, or reordered, place the active contour at the final measured geometry immediately so structural layout changes cannot produce a stale or offset animation. Keep resize and scroll geometry corrections immediate, and disable movement under reduced-motion preferences.
- Extend the active-tab fill from the shoulder tangent through the final half pixel of the tab strip so it overlaps the same-color address-toolbar background. This overlap must eliminate any white hairline without creating a visible foot at either shoulder.
- Overlap the address-toolbar background upward by half a CSS pixel at the tab baseline so no white hairline appears beneath an active tab. Tab-title line boxes must preserve Latin descenders such as `g`, `p`, `q`, and `y` without clipping.
- Let users drag page tabs horizontally to reorder them without reloading pages or losing per-tab state. Treat unused browser-shell space in the tab strip and address toolbar as a native window drag region, while every tab, button, input, menu, and other interactive control remains a no-drag region.
- Keep a one-click “总结当前页面” tool in the address toolbar. Clicking it immediately extracts the current page text and calls the default bound API's fastest compatible non-reasoning text model; never open a question/input composer and never use or display thinking/reasoning. Show only the direct loading state, final summary, or honest error. Treat the extracted page text as the only source and do not send page screenshots, images, or image metadata to the model.
- Round both outer ends of the tab strip's continuous bottom vector seam downward into the address-toolbar region with 10 px curves: the left end transitions into the bookmarks-sidebar boundary and the right end transitions into the window edge. Keep the address toolbar itself full-width with square corners and extend the vector overlay so neither downward arc is clipped or hidden behind the toolbar.
- Do not draw a hard divider between the bookmarks sidebar and webpage surface. Center a 1 px shadow plane on the exact shared boundary using a `-0.5px` offset, then horizontally mirror the webpage surface's right-edge shadow into the bookmarks sidebar: `-2px 2px 4px rgba(147, 153, 147, 0.3)`. Do not use a zero-width box because Chromium omits its shadow. Fade the first 4 px of this vertical shadow in where it meets the continuous top contour so the left corner has no gap, hard elbow, or dark overlap. This must use exactly the same gray, opacity, blur distance, downward offset, and visual hardness as the right and bottom shadows without casting into the webpage.
- Use one non-stacking Atlas-like glass layer for the browser shell: the sampled warm off-white `rgb(250, 249, 246)` (`#faf9f6`) at 25% opacity. Apply it to the bookmarks sidebar, tab-strip outer backing, 6 px webpage-frame gap, and native bottom-corner masks, with macOS `under-window` vibrancy enabled so desktop colors remain clearly visible. Keep the complete address toolbar, all webpage canvases—including Brizo-owned new-tab/start and Brief pages—and every active or inactive page tab fully opaque. Active tabs continue following their page background color; inactive tabs remain `#e9ebe7`.
- Give the main webpage canvas its own continuous light-gray frame, visually joined to the page-tab contour and the bookmarks sidebar's right divider. Its upper-right corner must remain connected by extending the same frame down through the address toolbar into the webpage edge. Keep that inner frame flush to the sidebar divider, while leaving exactly 6 CSS px of white shell between the frame and the outer browser edge on the right and bottom. Apply the same geometry to Brizo-owned pages and isolated external webpage views.
- Constrain the address toolbar's page-following background to the inner webpage-frame width. It must meet the active-tab lower shoulder and the right frame line exactly, never painting into the 6 px outer shell. Clip that background to the toolbar's two 10 px upper corner arcs so it never paints outside them.
- Use matching 12 px rounded corners at the lower-left and lower-right of the webpage surface. The webpage content immediately below the address toolbar must keep square upper-left and upper-right corners. For external pages and navigation snapshots, force the visible bottom rounding with two topmost 14×14 px shell-colored native corner masks spanning the real surface boundary. Render only a blurred pale-gray shadow along those native curves, with no crisp stroke; do not rely on a parent View's border radius to clip WebContentsView children.
- Use the webpage surface's lower and right edges as the single shadow reference for the active tab, address-toolbar surface, bookmarks-sidebar boundary, Brizo-owned page canvas, isolated external webpage surface, and native corner masks. The complete top shape—including both upper corner turns and the active tab—must use one continuous zero-offset contour shadow; never stack a second rounded-toolbar shadow over it. Continue from both contour endpoints with symmetric 1 px vertical shadow planes and fade their first 4 px in to avoid visible joints. The remaining outer edges use a 2 px directional offset, 4 px blur, and `rgba(147, 153, 147, 0.3)` wherever renderer geometry permits; native masks must match it optically. For the active tab, apply the shadow only to its open contour path and never to the fill or its bottom joining edge, so no shadow appears across the middle where the tab meets the toolbar. No continuous hard frame or sidebar divider should remain.
- Render the active tab, tab-strip outer seam, address-toolbar left and right edges, webpage body left and right edges, both lower corners, and the webpage bottom edge as one closed SVG contour with a single stroke and shadow. Do not reconstruct any segment with independent pseudo-elements, container box-shadows, active-tab shadows, or native corner strokes; native corner masks may only provide transparent clipping fill above external `WebContentsView` content.
- Webpage favicons in the bookmarks library use a 5 px rounded-rectangle mask, while top-tab icons retain their circular mask. Image favicons fill and crop responsively from the center without stretching, while fallback glyphs scale proportionally inside the matching mask.
- Use `src/compass-alt.svg` as the default icon for every blank/new-tab page, rendering it at 80% of the normal icon size in deep gray. Reserve `src/anchor.svg` for external webpage tabs whose current navigation is in an error state, rendering the anchor at 80% of the normal tab-icon size. Keep every top-tab icon container background fully transparent, including external webpage favicons and fallback glyphs; do not add colored, gray, white, bordered, or shadowed icon tiles behind them.
- Keep the active bookmark synchronized with the real external page: after navigation, update its title, domain, URL, and favicon from the loaded page rather than leaving the original sample metadata in place.
- Bind every external navigation and subsequent page metadata update, including computed page background color, to the tab and navigation generation that initiated it. During webpage-to-webpage navigation or refresh, freeze the outgoing viewport as a temporary visual snapshot and render the incoming document at full viewport size outside the visible shell; replace the snapshot directly only after the incoming document completes its paint-ready frames, never inserting a white screen or centered loading logo. For snapshot-backed replacements, sample the incoming rendered pixels and reject an empty white canvas as not yet ready. Keep target-frame readiness bounded by a short hard timeout so a site that suspends animation frames cannot block forever. Show loading only as one slightly thicker gold outline drawing clockwise from the top of the address field: it may approach but never close or restart while loading, then advances from its current position to exactly one complete 360-degree outline when the incoming document becomes paint-ready and fades out. Never use a repeating progress loop or wait for every late subresource request. Never let stale title, URL, favicon, or asynchronous background samples overwrite another tab, especially with the hidden `example.com` loading fixture. Selecting an already loaded live tab must reveal and remain on that tab's own retained document immediately.
- Give every external webpage tab its own isolated `WebContentsView` within the shared persistent normal-profile session. Switching a live tab only hides/shows views and must preserve DOM state, form state, media state, history, and scroll position without calling `loadURL` again. Enable background throttling for every hidden view, release views after 10 minutes of inactivity, and recreate them from the tab URL only when a released tab is selected again; closing a tab immediately destroys its view.
- When address input becomes a recognizable website, preconnect one socket through the persistent browser session after a short debounce. Keep page-background sampling and other non-critical metadata work off the first rendered frame.
- The desktop browser toolbar has no bottom divider. Its background follows the active external page's computed background color and transitions smoothly whenever that color changes.
- Match the address toolbar and active-tab fill to the dominant visible background of the webpage element that actually touches the top edge of the external page viewport (for example, a full-width banner or header). Verify top-edge contact geometrically rather than adopting the color of a nearby element with a gap; fall back to the document background when no opaque edge-touching element is present. Resample after scrolling and after delayed CSS, hydration, SPA navigation, or other DOM/style changes that can alter the top-edge element.
- Use `rgb(252, 250, 250)` (`#fcfafa`), sampled to match the top edge of `background.png`, for the initial new tab's active tab fill and complete browser toolbar; external webpage tabs continue to follow their computed page background colors.
- When the active external page background is dark, automatically use a light foreground for the active tab title and close control, the new-tab button, and the address-toolbar navigation and action icons. Determine this from computed color contrast, keep inactive pale-gray tabs dark, and leave the light address input surface and its contents unchanged.
- Keep browser scrolling on Chromium's native smooth-scrolling and GPU-composited path. Prioritize the display's native refresh rate while the external page is visible, and re-enable background throttling when that view is hidden.
- Immediately open the text-action context menu when a user finishes highlighting non-empty text on an external webpage, including pointer and keyboard selection completion. Use 5 px outer corners and 3 px item corners, and measure the rendered labels before showing so the menu width fits its contents within sensible viewport limits.
- Use one scrollbar language across external webpages, the bookmarks library, and the page-tab rail: the track and corner remain fully transparent, the thumb becomes clearer while scrolling, and after 520 ms of inactivity it returns to a darker 50%-transparent state.
- In browsing and loading states, show only the centered URL host, preserving `www` when present and any non-default port while hiding `http://`, `https://`, path, query, and fragment. Remove the address field's border, background, loading outline, and security dot completely so only the host appears to float on the toolbar. On focus, restore the full URL, left-align it for editing, and automatically select it all for replacement.
- On pointer hover, give the otherwise transparent browsing-state address display a subtle translucent tint—soft gray on light toolbars and soft white on dark toolbars—without restoring a border. Fade back to transparent when the pointer leaves.
- While typing a recognizable website or domain fragment in the top address field, show a dropdown of completed website addresses followed by their site names. Ensure this dropdown renders above the isolated external webpage surface rather than being occluded by it. Also show a compact 20 px circular right-arrow submit button inside the field, using the same pale-gold resting and Brizo-logo-gold hover/focus color system as the new-tab submit button.
- Focusing or clicking the browsing-state address display must only restore and select the full URL; it must not open suggestions, capture a page snapshot, hide the native webpage view, or cause the webpage to flash. Start autocomplete and its snapshot-backed overlay only after the user actually changes the address text.
- Match the top address field's entered text to the new-tab prompt input at 14 px, while keeping autocomplete suggestions at 12 px for primary text and 10 px for secondary site labels without changing the field or dropdown dimensions.
- Treat natural-language text in the top address field like the new-tab command field: provide the same personalized and region-aware online autocomplete/correction results. Submitting natural language must create and select a new Brizo tab, then immediately show its Brizo Scout AI loading/result view; recognized URLs continue to navigate directly.
- Keep the browser navigation controls, compact address field, bookmark action, and AI action centered as one toolbar group. The address field is roughly half its original width; the three-dot page menu remains independently right-aligned.
- Keep a one-click clean-article PDF action immediately to the right of the address field. It must extract the page title, author, readable body, meaningful images, and semantic emphasis while excluding navigation, ads, sidebars, and controls. Exported English text uses Times New Roman and Chinese text uses Songti/SimSun, with a consistent article hierarchy.
- PDF article titles must be selected from multiple signals: dedicated article metadata, structured data, Open Graph/Twitter metadata, visible article headings, Readability, and finally the document title. Treat a title equal to the site name as a low-confidence candidate, and prefer independently corroborated article-specific candidates.
- The browser and product name is Brizo across all user-facing interface, window titles, private browsing, About, AI, metadata, and launch surfaces. Preserve legacy `bean:` storage keys, browser partitions, and IPC channel names solely for data compatibility.
- Use Source Han Serif SC for Chinese glyphs and bundled EB Garamond for Latin glyphs throughout Brizo's renderer UI, including inputs, search results, menus, tabs, bookmarks, settings, and built-in content surfaces. Apply a 112% size adjustment to the EB Garamond face only, so its visual size matches Chinese without changing Chinese layout. External webpages and clean-article PDF typography remain independent.
- Route `example.com` and `www.example.com` over HTTP or HTTPS to Brizo's local Loading page rather than loading the external IANA site. That page is intentionally minimal: a pure-white page with the unmodified project-root `logo.png` centered, with no text or other chrome in the page content.
- Show every unreadable external page—including HTTP 4xx/5xx responses, DNS and connection failures, blocked access, offline state, and excessive loading delay—as a Brizo-owned error page. Use project-root `404.png` as a cover background at 20% opacity, keep `logo.png` at the new-tab logo position, and place the actual status/error reason plus one concise Chinese and English explanation below it while retaining the originally requested URL in the address bar.
- Use an opaque medium-gray backing (`#9ca19c`) behind real webpage favicon images in page tabs and the bookmarks library, so transparent PNG areas render consistently; retain the existing fallback-icon backgrounds.
- On external webpages, right-clicking an image opens a Brizo-owned overlay menu with exactly these actions: 新标签页中打开图片、下载该图片、复制该图片、复制图片地址. Opening creates and selects a real Brizo page tab; downloads flow through Brizo's normal download history. Keep this menu above the isolated webpage view and use the same Source Han Serif SC Chinese / bundled EB Garamond Latin typography as every other Brizo menu, including the Latin face's 112% size adjustment. The image menu has no outer frame, border, surrounding inset, or window shadow, and the entire menu renders at 95% opacity. Reveal it smoothly from the upper-left origin toward the lower-right using a short scale-and-clip animation, while respecting reduced-motion preferences.
- Use that same Brizo-owned 95%-opaque, frameless, shadowless, upper-left-to-lower-right animated design for every external-webpage context menu. Right-clicking a hyperlink offers 在新标签页打开链接 and 复制链接地址. Right-clicking a normal text selection offers 复制文字、向 Brizo 询问、翻译; asking creates a selected Brizo Scout AI tab, while translating uses the bound fast model and the session's single IP-country lookup to place the local-language translation directly below the original selection. Format the translation card as two separated rows: an italic original wrapped in Chinese double quotation marks on top, and the translated result below. Show an honest inline error in the result row when no model is bound or translation fails.
- Replace the address-toolbar AI sparkle action with a Downloads control. On desktop, open the download history as an independent native popover directly below the button; horizontally center it on the button when it fits, otherwise align it to the window's right edge with an 8 px inset. It overlays without hiding, resizing, or otherwise affecting the webpage. Record normal-profile downloads locally with filename, source URL, save path, and timestamp; group the panel by 今日、昨日、本周、本月、历史. Re-check file existence whenever the panel is opened and strike through every displayed text field when a completed file was deleted or moved. Keep downloads from private windows out of this local history.
- Automatically open the Downloads popover when any normal-profile image or file download completes, and close it when the user clicks elsewhere. Each visible download row shows only the filename and save path: use a real local thumbnail at left for images and a file glyph for other types. Clicking the row opens the downloaded file; a small trailing folder button reveals it in the system file manager. Missing files remain struck through and cannot be opened.
- Keep the rightmost three-dot menu as Brizo's complete settings and page-tools hub: local account profile, one-step bookmark import from installed browsers or bookmark HTML, folder/smart-category organization, native printing, selected-area/visible-page/scrolling-full-page screenshots, an isolated in-memory private window, and app/about information. Browser imports and captured files stay local unless the user explicitly moves them.
- Organize the three-dot hub as a first-level menu opening consistent second-level dialogs. Include first-level entries for 历史浏览记录, 历史搜索记录, 密码保险箱, and 设置. Browser history records only normal-profile real page visits locally; search history shows locally persisted queries/result snapshots; the password vault must use an honest empty state until secure save/autofill is implemented; Settings includes language preference, a native selectable download directory that affects subsequent downloads, automatic-update preference, and version/about access. Every second-level dialog provides a back action to the first-level menu.
- Use the project-root `logo.png` as Brizo's primary graphic mark and `logo brizo.png` as the wordmark shown beside it. Preserve both transparent PNG assets without recoloring, distortion, or recreating them with icons or text; use the graphic mark alone where only an icon fits.
- In the expanded bookmark-sidebar header, display the standalone `logo.png` graphic mark at 38 px and the `logo brizo.png` wordmark at 70 px, preserving their aspect ratios and horizontally centering the combined group in the sidebar. In the collapsed 60 px rail, hide the wordmark but keep the standalone `logo.png` centered at the top.
- Use project-root `logo app.png` as the sole source for all externally exposed application icons, including packaged executables, installers, macOS Dock/Finder, Windows Start menu and shortcuts, Linux launchers, and Electron windows. Generate platform-specific icon formats from this source without distortion, recoloring, or replacing it with an interface logo.

## Brizo Brief durable decisions

- Keep one fixed, non-draggable, non-closable `Brief` short tab immediately after the new-tab `+`. Brief is an independent built-in surface and must not enter the normal webpage-tab array; startup still selects exactly one blank new tab.
- Keep the edition name (for example `THE MORNING POST`) geometrically centered across the webpage regardless of the left and right masthead content. Use `BrizoAI` as the masthead scope label, show no editorial mission subtitle, and do not show a “更多精彩内容”/later-pages preview on page 1.
- Page 1 is the cross-category headline page. Every displayed headline, summary, detail report, and related-news card must be Chinese; retain links to every original source.
- Drive every refresh with six concurrent Serper.dev News queries spanning technology, business, international, domestic, culture, and sports/entertainment. Use Chinese queries with China/Chinese search parameters for China-facing desks and an English query with US/English parameters for the international desk. Do not call 博查 for Brief; RSS/RSSHub/Horizon feeds are resilient fallbacks only when Serper returns too few usable authoritative articles.
- Every transition into Brief from another surface, every click on the already-selected Brief tab, and every manual refresh triggers a forced background refresh. Keep the latest successful edition visible immediately, show refresh state without blocking, and replace it only after the new edition completes.
- Keep Brief source retrieval and ranking deterministic: enforce the authoritative news allowlist, prefer article URLs, cluster the same event, deduplicate by URL/domain, rank by authority, freshness, relevance, and topic weight, and never fabricate a story, source, publication time, or image. Use the bound fast model only for foreign-to-Chinese translation and clicked-story multi-source synthesis.
- Render Brief as a continuous native-scrolling Discovery-style editorial stream while preserving Brizo's light shell and typography: a large text-left/image-right lead, a row of three image cards, then varied wide and compact image stories. Titles should occupy about two to three lines, summaries about two to four lines, and every story footer must show stacked real publisher favicons, source count, and `已发布 X 分钟/小时/天前`; keep the existing category rail and omit market widgets.
- Use `THE MORNING POST` from 09:00–17:59, `THE EVENING POST` from 18:00–23:59, and `OVERNIGHT UPDATE` before 09:00. Preserve a stale successful edition when refresh fails and never replace missing real sources with fabricated news or images.
- Opening a story must immediately show its cached headline, summary, publication time, source stack, and main image. In parallel, enrich up to six distinct authoritative sources and use the bound fast model to produce a five-to-eight-paragraph Chinese synthesis with numbered inline citations, clearly preserving uncertainty or source conflicts. Do not show thinking. If synthesis fails, keep the immediate story view and show an honest inline error or cited extractive fallback.
- A completed story report may show up to three real source images and must end with exactly five best available related image-news summaries, ranked by topic similarity and freshness. Clicking a related item stays inside Brief and immediately opens that story; closing or pressing Escape restores the stream position.
- Brief personalization may send bounded recent bookmark titles/folders/domains, normal browsing titles/domains/counts, and search/question queries to the bound default model. Never send full URLs, webpage or answer bodies, attachments, credentials, private-window records, or API keys.
- Do not show “为什么推荐” anywhere in Brief. Every newspaper story uses the strict content anatomy: a real source image, a concrete Chinese headline, a factual body excerpt, source count, and publication metadata. Existing local 置顶、减少推荐 and 屏蔽 preferences continue to influence ranking even though the visible right rail is a compact category switch.
- Keep Brief stories and citations on an allowlist of authoritative and popular news domains (wire services, major global/China newsrooms, finance, tech, science, arts, and sports outlets). Hard-reject knowledge bases, Q&A, wikis, encyclopedias, forums, social feeds, ecommerce, company registries, search/portal homepages, and similar non-news sources such as Wikipedia, Zhihu, Baidu Baike, Quora, Reddit, CSDN, and Google/Bing result hubs. Prefer article URLs over section homepages.
- Keep the initial edition path fast: run six Serper News calls concurrently, bound metadata enrichment to the highest-ranked candidates, cache feed responses and metadata, retain Chinese text without a model call, and batch only foreign-language items for translation.
- Preserve the existing Brief masthead and category sidebar when correcting retrieval, ranking, or source data. Do not seed FT/FT中文网 through preview fixtures, search queries, RSS presets, or RSSHub routes; FT remains eligible only when Serper returns a genuinely relevant article organically.
