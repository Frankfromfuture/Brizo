// Shared by the Ask composer and the browser-owned Use/Agent running effect.
export const ASK_BEAM_PRESET = Object.freeze({
  colorVariant: "sunset",
  size: "md",
  strength: 0.85,
  theme: "dark",
});
