# Brizo 中国网页专项优化升级计划

## 总体目标

采用“先 Brizo/Electron 外层优化，数据证明必要后再浅层分叉 Chromium”的路线。macOS 先行，默认保守兼容，性能数据仅保存在本地。

第一阶段验收指标：

- 60 个代表性中文网站中位 LCP/可见首屏时间改善 ≥15%。
- 10 标签稳态内存下降 ≥20%。
- 导航失败率增加不超过 0.5 个百分点。
- 冷启动回退不超过 5%，安装体积增加不超过 5%。
- 不降低 sandbox、context isolation、Site Isolation 和权限防护。

## 核心实施

### 1. 本地性能实验室

- 建立 60 站点基准集：资讯10、视频8、电商8、社区8、搜索/地图6、政务/银行10、旧编码5、重型SPA5；仅选无需登录的公开页面。
- 通过 Electron 导航事件、CDP Performance/Network、`app.getAppMetrics()` 采集 DNS/连接、TTFB、FCP、LCP、CLS、长任务、请求数、传输量、CPU、内存、GPU、失败类型和恢复耗时。[Electron 可用进程指标](https://www.electronjs.org/docs/latest/api/app)
- 每个场景执行5轮冷缓存和5轮热缓存，报告中位数与P95，并保留优化前基线。
- 数据以版本化 JSONL 保存在兼容 userData 目录；只记录 origin，不保存查询参数、页面正文、表单、Cookie或凭证。
- 新增本地“性能实验室”设置页，支持运行基准、查看差异和手动导出；绝不自动上传。

### 2. 网络与中文首屏

- 将现有单次预连接升级为本地学习策略：记录每个 origin 的连接耗时及稳定的首屏关键 origin，地址确认后最多预连接2个高置信目标，每个1个 socket。
- 建立 DNS、TLS、超时、拒绝连接、HTTP错误和内容阻断的独立诊断；仅对幂等 GET 主导航执行一次受控重试，不重试 POST、支付、下载或上传。
- 使用站点历史P95形成8–20秒的自适应超时；无历史时沿用20秒。
- 默认不替换系统 DNS、不代理流量、不清缓存、不阻断第三方资源。
- 增加可选“智能模式”：仅延迟不可见 iframe、自动播放媒体和已确认不影响登录/支付的第三方资源；异常时一键切回标准模式，并记录站点级覆盖。
- 增加中文专项测量：字体发现、字体回退、首段排版、远程字体阻塞、简繁日字形选择、中文IME composition、GBK/GB18030声明与乱码诊断。
- 第一阶段不强制改写网站字体或转码；只有字体/编码占目标站点首屏瓶颈超过10%时才进入内核候选。

### 3. 标签生命周期与稳定性

- 将固定10分钟释放升级为四态：
  - `Active`：当前可见，原生刷新率与GPU路径。
  - `Warm`：隐藏0–3分钟，启用后台限速但保留完整DOM状态。
  - `Cold`：隐藏3–10分钟，降低优先级并释放非关键预览缓存。
  - `Released`：保存URL、历史位置、滚动位置和安全截图后销毁 View；重新选择时恢复。
- 音频播放、下载、媒体会议、未提交表单和固定标签不得自动释放；只记录“存在未提交内容”，不保存表单值。
- 每2秒读取进程指标；系统可用内存低于10%或 Brizo RSS 超过物理内存25%时，按最近使用顺序提前释放 Cold 标签，最低保护当前标签和最近两个标签。
- 接入 `render-process-gone`、`unresponsive/responsive`、`child-process-gone`：单标签崩溃只重建对应 View；网络服务或GPU异常显示明确原因并提供恢复。[Electron WebContents 恢复能力](https://www.electronjs.org/docs/latest/api/web-contents)
- 同一标签两分钟内连续崩溃两次时进入站点兼容模式；GPU进程连续崩溃时提示用户一次性以软件合成重启，不静默永久关闭硬件加速。
- 保留现有错误页、原请求地址、下载记录和独立标签状态，内部 `file://` 地址不得泄漏。

### 4. 安全与视觉性能

- 保持外页 Node Integration 关闭、sandbox/context isolation 开启、私密窗口内存分区、外部页面独立 View。
- 建立权限代理：
  - 永久默认拒绝 WebUSB、WebSerial、蓝牙、MIDI、传感器和任意文件系统访问。
  - 摄像头、麦克风、通知、定位仅允许明确的逐站授权，可随时撤销。
  - 证书错误默认不绕过；下载校验 MIME、扩展名、来源和最终保存路径。
- 内置页与外部页使用独立 CSP、导航白名单和 IPC 发送者校验；新接口继续保留 `bean-browser:*` 兼容命名。
- 不强制创建合成层；记录掉帧、主线程长任务、栅格和GPU异常，仅对已证明安全的动画采用 compositor 路径。
- 智能模式在掉帧持续超过3秒时可降低不可见动画和自动播放质量；正文、输入、滚动和可访问性优先。

## 接口与本地数据

- 新增 `PerformanceSample`：版本、时间、平台、Electron/Chromium版本、origin、优化模式、导航指标、Web Vitals、请求摘要、进程摘要、失败与恢复结果。
- 新增 `SiteOptimizationPolicy`：origin、`standard | smart | compatibility`、更新时间和自动回退原因。
- 新增主进程能力：读取性能摘要、运行/取消基准、导出报告、读取/修改站点模式、读取标签生命周期与故障状态。
- 所有数据写入 `userData/performance/`；滚动快照、故障记录和策略设上限并循环清理，默认保留30天。
- 设置页提供总开关、当前站点模式、性能实验室、数据清除和报告导出；默认模式为 `standard`。

## Chromium 分叉准入与交付阶段

1. **基线阶段（2–3工程周）**：完成指标、基准集、本地报告和优化前数据。
2. **外层优化阶段（4–6工程周）**：网络学习、标签生命周期、故障恢复、安全权限和智能模式。
3. **macOS Beta（2–3工程周）**：小范围本地验证，达到全部平衡指标后进入稳定版。
4. **跨平台迁移（3–5工程周）**：Windows/Linux 复测内存阈值、GPU恢复、字体和网络差异。
5. **内核评审门**：只有外层阶段未达到15%首屏目标，且分析证明至少20%瓶颈位于 Blink/Skia/V8 内部时，才创建 Electron/Chromium实验分支。
6. **浅层分叉约束**：补丁总量控制在约2000行、全部置于运行时 Feature Flag、每周同步上游、关键安全更新72小时内合并；禁止删除 Web 标准和关闭站点隔离。
7. **候选内核补丁**：中文字体回退缓存、常用汉字栅格预热、CJK首屏排版增量化、旧编码高置信诊断、关键资源调度；逐项A/B，不打包成不可拆分的大补丁。

## 测试与发布门槛

- 单元测试：指标计算、隐私裁剪、网络重试、站点策略、内存状态机、权限决策、崩溃循环。
- Electron 集成测试：慢响应、断网、DNS失败、TLS失败、4xx/5xx、重定向、GBK页面、超大中文字体、SPA、视频、未提交表单、渲染器/GPU故障。
- 视觉测试：简繁中文、IME、字体回退、滚动、动画、深浅顶部工具栏和错误页。
- 性能回归：60站点基准、启动基准、10/30标签内存、滚动掉帧、切换与恢复耗时。
- 安全回归：权限拒绝、私密分区、跨标签状态隔离、内部路径隐藏、IPC来源校验、危险下载。
- 任一关键登录/支付/政务页面出现功能破坏、任何安全边界降低或失败率超过阈值，立即关闭相应 Feature Flag，不阻塞整版回滚。

## 已确定的默认决策

- macOS 先行，随后迁移 Windows/Linux。
- 第一阶段不维护 Chromium 分叉。
- 标准模式默认开启；智能模式由用户主动选择或逐站授权。
- 性能数据仅保存在本地，只有用户主动导出才离开设备。
- 不以删除冷门网页标准换取体积；优先优化实际中国网页瓶颈。
- 保留 legacy `bean:` 存储、分区及 IPC 名称，避免破坏现有数据。
